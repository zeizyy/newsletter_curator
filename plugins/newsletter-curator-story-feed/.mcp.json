{
  "mcpServers": {
    "newsletter-curator-story-feed": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "python",
        "../../scripts/newsletter_mcp_launch.py",
        "--local-config-path",
        "../../config.yaml"
      ],
      "note": "Read-only MCP server for stored newsletter stories. By default this launches the repo-local SQLite-backed server; set CURATOR_MCP_TARGET=ssh plus CURATOR_MCP_SSH_HOST and CURATOR_MCP_REMOTE_REPO_DIR to run the same server on the real curator host over SSH stdio."
    }
  }
}
